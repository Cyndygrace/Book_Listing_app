//require the main graphql
const graphql = require('grapgql');

//in creating our  schema, Our schema will define the type of data, object types, relationship and how we want our data to interact and how to query them

//We destructure graphql to obtain the graphql object data template
const {GraphQLObjectType} = graphql;
